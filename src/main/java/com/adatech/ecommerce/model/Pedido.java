package com.adatech.ecommerce.model;

import java.math.BigDecimal;
import java.time.LocalDate;
import java.util.List;

/**
 * Representa um pedido de venda no sistema.
 * TODO:
 *  - Adicionar atributos: id (int), cliente (Cliente), dataCriacao (LocalDate), itens (List<ItemVenda>), valorTotal (BigDecimal), status (StatusPedido).
 *  - Criar construtor para inicializar os atributos. O status deve ser "ABERTO" por padrão.
 *  - Criar getters e setters.
 *  - Criar método para adicionar item ao pedido.
 *  - Criar método para remover item do pedido.
 *  - Criar método para calcular o valor total do pedido.
 *  - Sobrescrever o método toString() para uma representação textual do pedido.
 */
public class Pedido {
}

