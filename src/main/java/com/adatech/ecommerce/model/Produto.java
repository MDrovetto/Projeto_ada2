package com.adatech.ecommerce.model;

import java.math.BigDecimal;

/**
 * Representa um produto no sistema.
 * TODO:
 *  - Adicionar atributos: id (int), nome (String), descricao (String), preco (BigDecimal), estoque (int).
 *  - Criar construtor para inicializar os atributos.
 *  - Criar getters e setters para todos os atributos.
 *  - Sobrescrever o método toString() para uma representação textual do produto.
 */
public class Produto {
}

